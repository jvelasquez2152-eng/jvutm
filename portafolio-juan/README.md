# Portafolio — Opción D (Juan Velásquez)

Tema oscuro elegante con acentos lima y cyan. Estructura renovada: héroe con CTA, sobre mí, servicios, proyectos, testimonios y contacto.

## Cómo correr
Solo abre `index.html` en tu navegador.

## Cómo subir a Git
```bash
git init
git add .
git commit -m "feat: portafolio de Juan Velásquez — opción D"
git branch -M main
git remote add origin <URL_DE_TU_REPOSITORIO>
git push -u origin main
```
